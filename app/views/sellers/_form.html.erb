<%= form_for @seller do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name, class: 'form-control' %>
  <%= error_for :name, @seller.errors %>
  <br>
  <%= form.label :rating, 'Rating' %>
  <%= form.text_area :rating, class: 'form-control' %>
  <%= error_for :rating, @seller.errors %>
  <br>
  <%= form.submit params[:action] == 'new' ? 'Save' : 'Update', class: 'btn btn-block btn-primary' %>
<% end %>
