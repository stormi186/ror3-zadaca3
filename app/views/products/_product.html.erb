<tr>
  <td><%= link_to product.name, product_path(product) %></td>
  <td><%= product.price %></td>
  <td><%= link_to seller_for(product), seller_path(product.seller_id) %></td>
  <td><%= link_to category_for(product), category_path(product.category_id) %></td>
  <td><%= time_for(product) %></td>
  <td>
  	<div class="btn-group">
    	<%= link_to 'Edit', edit_product_path(product), class: 'btn btn-sm btn-primary' %>
    	<%= link_to 'Delete', product_path(product), method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-sm btn-danger' %>
    </div>
  </td>
</tr>
